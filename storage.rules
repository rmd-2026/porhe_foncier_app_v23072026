rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /documents/{parcelleId}/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
  match /suivi_visuel/{parcelleId}/{fileName} {
    allow read, write: if request.auth != null;
  }
}